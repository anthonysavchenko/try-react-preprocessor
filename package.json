{
  "name": "try-react-preprocessor",
  "scripts": {
    "build": "npx babel src --out-dir temp --presets react-app/prod && npx terser -c -m -o like_button.production.min.js -- temp/like_button.js"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2",
    "terser": "^5.14.0"
  }
}
